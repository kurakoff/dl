services:
  backend:
    build: ./backend
    env_file: .env
    volumes:
      - seo-data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: ""
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  seo-data:
